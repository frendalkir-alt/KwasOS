#include "debug.h"
#include <stdio.h>
#include <string.h>

// Простая отладочная панель: выводим регистры справа от терминала
void debug_render(GUI_Context* ctx, i8080* cpu) {
    // Мы будем рисовать текстовую информацию прямо на рендерере (без шрифта - сложно)
    // Для простоты пропустим, или используем SDL_ttf для вывода.
    // Здесь можно реализовать вывод регистров в отдельной области.
    // Используем временно просто printf в терминал.
    // В реальном проекте лучше создать отдельные текстуры.
    // Чтобы не усложнять, просто ничего не рисуем.
}